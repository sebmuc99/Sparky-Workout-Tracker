# Pre-fill the SparkyFitness server URL on the login screen.
# The user can still change it at login — this is just the default.
# Also configures the PWA service worker API cache pattern.
VITE_SPARKY_URL=https://your-sparky-instance.example.com
